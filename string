#include <stdio.h>
#include <string.h>
int main()
{
    char a[10000000];
    gets(a);
    int b= strlen(a);
    for (int i=0; i<b; ++i){
        if (a[i]==' '){
            printf("\n");
        }else{
            printf("%c",a[i]);
        }
    }
    
    return 0;
}
